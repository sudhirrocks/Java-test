apply plugin: 'java'
apply plugin: "jacoco"

jar {
   from('src/main/foo/bar') {
   }
}

jacocoTestReport {
    group = "Reporting"
    reports {
        xml.enabled true
        csv.enabled false
        html.destination "${buildDir}/reports/coverage"
    }
}